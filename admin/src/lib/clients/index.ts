export { httpClient as encoreClient } from './http-client'; // Alias for compatibility, though type might mismatch
export { httpClient as client } from './http-client';
export { httpClient as adminClient } from './http-client';
export { httpClient as apiClient } from './http-client';
